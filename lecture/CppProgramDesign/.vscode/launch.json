{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug with lldb",
      "type": "lldb",                 // 依赖 CodeLLDB 插件
      "request": "launch",
      "program": "${fileDirname}/${fileBasenameNoExtension}",
      "args": [],
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "clang++ build current file"
//      "miDebuggerPath": "/usr/bin/lldb", // 一般不用改
//      "setupCommands": [
//        {
//          "description": "Enable pretty-printing for clang",
//          "text": "settings set target.prefer-dynamic-value run-target"
//        }
//      ]
    }
  ]
}